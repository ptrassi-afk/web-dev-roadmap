# Boilerplate

I used a template called 'boilerplate', which helps in creating web pages by providing a predefined structure. I used the 'DOCTYPE' declaration to inform the browser of the type of HTML used and the 'html' element, responsible for organizing and structuring the page content.